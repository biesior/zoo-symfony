<form method="post" action="{{ path('caretaker_delete', {'slug': caretaker.slug}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" style="display: inline-block;">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ caretaker.id) }}">
    <button class="btn btn-danger btn-sm">Delete</button>
</form>
