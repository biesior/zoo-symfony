{% extends 'base.html.twig' %}

{% block title %}Edit Cage | {{ zoo_titlename }}{% endblock %}

{% block body %}
    {{ include('_partials/jumbo.html.twig', {'header1': 'Edit cage\'s details' , 'subheader': 'Make this room nice and comfortable'}) }}
    <div class="container">
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="{{ path('index') }}">Home</a></li>
                <li class="breadcrumb-item"><a href="{{ path('cage_index') }}">Cages</a></li>
                <li class="breadcrumb-item"><a href="{{ path('cage_manage') }}">Manage</a></li>
                <li class="breadcrumb-item active" aria-current="page">{{ cage.name }}</li>
            </ol>
        </nav>

        {{ include('cage/_form.html.twig', {'button_label': 'Update'}) }}
        <br><br>
        <a href="{{ path('cage_manage') }}" class="btn btn-secondary btn-sm">Manage other animals</a>
        <a href="{{ path('cage_index') }}" class="btn btn-sm btn-secondary">Back to preview</a>

        {{ include('cage/_delete_form.html.twig') }}
    </div>
{% endblock %}
